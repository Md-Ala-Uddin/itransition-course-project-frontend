/**
 *  @fileoverview shows sortable table of templates (create new, delete, or edit) and the sortable table of the filled forms (probably on two separate tabs).
 *
 */
export default function UserDashboard() {
    return <div className="">User defined templates and forms</div>;
}
