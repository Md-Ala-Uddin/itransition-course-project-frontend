/**
 * @fileoverview Page top Header
 * @description provides access to the full text search
 * @description Search results are always templates (e.g., if text is found in the question description or template comments, the search result links to the template itself, not to the question or comment).
 *
 */
export default function PageHeader() {
    return <div className="">Navbar</div>;
}
